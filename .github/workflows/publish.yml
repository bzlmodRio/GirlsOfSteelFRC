on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: bzlmodrio/vendor-json-repo/.github/actions/add_vendordep@ef8ce907fbdb333bdfc4768aab0830b72608846c
        with:
          token: ${{ secrets.VENDOR_DEPLOY }}
          vendordep_file: ../rev.json
          pr_title: "DO NOT LAND - testing auto-publish action"
          pr_branch: "testing_update_v99"
          is_beta: true
          fork_organization: bzlmodrio
