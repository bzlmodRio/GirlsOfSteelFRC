on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: bzlmodrio/vendor-json-repo/.github/actions/add_vendordep@ac0a35967bb2f3231d2ab1a22f09ab78a9c9839e
        with:
          token: ${{ secrets.VENDOR_DEPLOY }}
          vendordep_file: ../rev.json
          pr_title: "DO NOT LAND - testing auto-publish action"
          pr_branch: "testing_update_v99"
          is_beta: true
          fork_organization: bzlmodrio
